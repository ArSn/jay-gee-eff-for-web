checks:
    javascript: true
build:
    nodes:
        analysis:
            dependencies:
                before:
                    - 'nvm install 16.14.2'
            tests:
                override:
                    - js-scrutinizer-run
filter:
    excluded_paths:
        - "coverage/" # only relevant on dev
        - "demo/"
        - "node_modules/"
        - "test/"
